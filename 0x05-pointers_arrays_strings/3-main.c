#include "main.h"
#include <stdio.h>

/**
 * main - entry point for imported functions
 * 
 * Return: 0 Always
 */

int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
